# Product polish verification — 2026-09-16

The existing report → claim → rescue → adoption workflow remains intact. Three parallel implementation workstreams covered reporting reliability, case clarity, and map recovery; integration added account-cache isolation and corrected location-access wording.

## Automated verification

- Full Jest suite: 124 tests across 12 suites passed.
- Application and test TypeScript checks passed.
- Web export passed.
- Lint: no errors; five existing effect-state warnings remain in the map, settings, and count-up hook after fixing a test import warning.
- Local Expo server: `http://localhost:8082`; page and JavaScript bundle returned HTTP 200. Browser opening succeeded through the OS.

## Behaviour covered by regression tests

- Draft restore waits for hydration, serializes writes/clears, isolates accounts, rejects corrupt data, and preserves work after storage failures.
- Case guidance follows existing statuses and distinguishes assigned guardians and adoption request outcomes.
- Concurrent location requests deduplicate; denied/revoked permissions remove stale coordinates; a failed location lookup can recover.
- Account changes clear private cached responses; late requests cannot restore the previous account's data; token refresh retains the same account's cache.
- Production analytics consumes lazy requests and contains transport failures.

## Remaining manual checks

No browser/native automation surface was connected, so HTTP/build checks are not a visual or end-to-end pass. Rehearse on desktop and a phone-sized viewport, plus actual iOS/Android devices:

1. Start a report, enter details and select a location, leave, and reopen. Details restore; photos must be reattached (explained in the UI). Discard and reopen to check clearing.
2. Fail a photo upload, retry, and confirm only successful submission clears the draft. Check camera/library cancellation and denial. UI submission locks do not provide server idempotency for an ambiguous lost response.
3. Open an unavailable report; retry/back controls remain usable. Complete the lifecycle with separate reporter, guardian, and adopter test accounts.
4. Deny location, use manual map navigation, retry location and settings recovery. Expand/hide the nearby list with keyboard/screen reader and drag on a device.
5. Switch accounts and verify private profile/location data is not retained in rendered screens.
6. Verify live push delivery/deep links and backend RLS/RPC tests against an isolated test backend.

No production migrations, backend data writes, or deployment were performed by this polish pass. Existing reporting rollback and ambiguous network-response behaviour still need backend-level idempotency work if guaranteed retry deduplication is required.
